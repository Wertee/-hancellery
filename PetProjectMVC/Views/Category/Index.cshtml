@model List<Category>

<a class="btn btn-primary" asp-action="AddCategory">Создать категорию</a>


@if(Model.Count == 0){
    <h3>Нет категорий</h3>
}else
{
    <table class="table">
    @foreach (var category in Model)
    {
        <tr>
            <td>@category.Name</td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-categoryId="@category.Id">Изменить</a>        
            </td>
        </tr>
    }
    </table>
}

